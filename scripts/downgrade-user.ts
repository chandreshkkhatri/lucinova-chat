import mongoose from "mongoose"; import dotenv from "dotenv"; import { User } from "../db/models"; dotenv.config(); async function run() { await mongoose.connect(process.env.MONGODB_URI!); const email = "chandresh.code@gmail.com"; const result = await User.findOneAndUpdate({ email: email.toLowerCase() }, { plan: "free", isPro: false, subscriptionId: null, subscriptionStatus: null, currentPeriodEnd: null }, { new: true }); console.log("User downgraded:", JSON.stringify(result, null, 2)); process.exit(0); } run();
